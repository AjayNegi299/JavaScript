//---------While Loop-----------

let index = 1
while (index<=10) {
    console.log(`value is ${index}`);
    index = index + 1
}

//While loop with array

    const myArray = ['akshay', 'akash', 'mayank'];
    let arr = 0;
    while (arr < myArray.length) {
        console.log(myArray[arr]);
        arr++
    }

    //do while loop
    let score = 11
    do {
        console.log(`value is ${score}`);
        score++
    } while (score<=10);
